let firstName: string = "Dylan"  // Explicit type writing out there type

console.log(typeof firstName)

let secondName = "Kalisa"  //implicit TypeScript will "guess" the type, based on the assigned value:

console.log(typeof  secondName);